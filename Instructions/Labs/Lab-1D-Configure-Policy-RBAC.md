---
lab:
  title: Azure Policy とロールベースのアクセス制御を構成する
  description: 組み込みの Azure Policy を割り当て、コンプライアンス評価を検証した後、Cloud Shell を使用し、Bicep を介してカスタムのタグ強制ポリシーをデプロイし、スコープ付きアクセス許可を持つカスタムの Azure ロールを作成し、Entra ID のアクセス レビューを使用して上昇特権ロールの割り当てを評価して修復し、リソース ロックを使用して、リソースを削除されないように保護します。
  level: 300
  duration: 60
  islab: true
  primarytopics:
    - Azure Policy
    - Azure Role-Based Access Control
    - Microsoft Entra ID Governance
---

# ラボのセットアップ

リソースをデプロイし、アクセス レビュー シナリオで使用されるロールの割り当てをシードする演習を開始する前に、次の手順を完了します。

1. **Azure portal ** (`https://portal.azure.com`) を開き、**User1** でサインインします。

1. ポータルの上部バーで、**[Cloud Shell]** アイコン (>_) を選択します。 メッセージが表示される場合は、**[Bash]** を選択します。

1. **User3** に指定したユーザー名をコピーします。 Cloud Shell で `<User3-UPN>` をそのユーザー名に置き換え、次を実行します。

    ```bash
    az ad user show --id '<User3-UPN>' --query id --output tsv
    ```

1. 返されたオブジェクト ID をコピーします。 これを展開テンプレートに提供します。

1. ラボ サブスクリプション ID から安定した 8 文字のサフィックスを生成します。

    ```bash
    LAB_INSTANCE_ID=$(az account show --query id -o tsv | tr -d '-' | cut -c1-8)
    echo $LAB_INSTANCE_ID
    ```

1. 表示されたサフィックスをコピーします。 これを**ラボ インスタンス ID** として提供します。

1. オンデマンド ポリシー スキャンに必要な Microsoft.PolicyInsights リソース プロバイダーを登録します。

    ```bash
    az provider register --namespace Microsoft.PolicyInsights --wait
    az provider show --namespace Microsoft.PolicyInsights --query registrationState -o tsv
    ```

    出力が `Registered` であることを確認してから続行します。

1. ポータル検索バーで、「**カスタム テンプレートのデプロイ**」を検索して開きます。

1. **[エディターで独自のテンプレートをビルド]** を選択してから **[ファイルの読み込み]** を選択します。

1. ラボの VM の **F:\AllFiles\Lab-1D** フォルダーから **1D-lab-env.json** を選択し、次に **[保存]** を選択します。

1. [デプロイ] ページで、次の値を構成します。

    | 設定 | 値 |
    |---------|-------|
    | **リージョン** | 米国東部。ラボ環境で他の地域が推奨されている場合を除く |
    | **ラボ インスタンス ID** | Cloud Shell から返される 8 文字のサフィックスを貼り付けます |
    | **User3 オブジェクト ID** | Cloud Shell コマンドで返されたオブジェクト ID を貼り付ける |

1. **[確認と作成](確認と作成)** を選択し、次に **[作成]** を選択します。

    > **注**: デプロイは通常 2 分以内に完了します。 これにより `sc500-lab1d-rg`、ポリシー評価に使用するタグ付けされていないストレージ アカウントと仮想ネットワーク、ラボ サブスクリプション上の **User3** の明示的な共同作成者割り当てが作成されます。

1. デプロイが成功したら、Cloud Shell を閉じて演習を続けます。

===

# Azure Policy とロールベースのアクセス制御を構成する

組織の AI プラットフォーム環境のコンプライアンス監査で、2 つのガバナンス ギャップが特定されました。 第一に、必要なリソースのタグ付けを強制するポリシーが存在せず、サブスクリプション内のリソースに一貫性のある `Environment` タグがないため、コストの割り当てとセキュリティ境界の追跡が信頼性に欠けます。 第二に、9 か月前に AI プラットフォーム チームを離れたチーム メンバーが、そのラボ サブスクリプションに対して有効な共同作成者割り当てを引き続き保持しており、現在は作業しなくなったリソースに対する完全な管理アクセス権が付与されたままです。

あなたの任務は両方のギャップを埋めることです。 非準拠リソースを明らかにする組み込みのタグ付けポリシーを割り当てた後、コードとしてのインフラストラクチャを介してカスタム ポリシーをデプロイして、タグの強制をリソースグループにまで拡張します。 セキュリティ レビュー担当者向けのスコープ付きカスタム役割を作成して、それらの担当者を管理者に昇格させることなく、Defender for Cloud 態勢のデータに対する読み取りアクセスとロールの割り当てを付与します。その後、Entra ID のアクセス レビューを使用して正式に共同作成者アクセスを評価し、不要なアクセスを削除します。 最後に、リソース ロックを適用して、プラットフォームのストレージ アカウントを誤って削除されないように保護します。

このラボでは、次のことを行います。

- 組み込みの Azure Policy を割り当て、ポリシー コンプライアンスの結果を確認する
- Cloud Shell を使用し、Bicep を介してカスタムのタグ強制ポリシー定義をデプロイする
- カスタムの Azure ロールを作成し、指定されたセキュリティ レビュー担当者に割り当てる
- Entra ID のアクセス レビューを使用して、上昇特権ロールの割り当てを評価して修復する
- CanNotDelete リソース ロックを適用し、それによって削除が防止されることを確認する

この演習の所要時間は約 **60** 分です。

> **メモ**: このホストされているラボでは、次の提供されたアカウント エイリアスとベースライン サブスクリプション ロールを使用します。**User1** (`sc500-user1-`、LOD 所有者)、**User2** (`sc500-user2-`、LOD 共同作成者)、**User3** (`sc500-user3-`、LOD 閲覧者)。 ラボ セットアップのデプロイで、ラボ サブスクリプションに User3 への組み込みの共同作成者の割り当てが追加されます。アクセス レビューによりその割り当ては削除されますが、User3 のベースライン LOD 閲覧者アクセスは保持されます。 3 つのアカウントすべてに指定されている認証情報を使用してください。

---

## 組み込みのコンプライアンス ポリシーを割り当てる

Azure Policy は、定義されたルールに照らしてリソースを評価し、遵守状況を報告します。既存のリソースを変更する必要はありません。 **拒否**効果ポリシーは、非準拠リソースの新規作成をブロックします。ポリシーに既に違反している既存のリソースは、コンプライアンス レポートで**非準拠**として表示されます。 組み込みの**リソースでタグを必須にする**ポリシーを `sc500-lab1d-rg` に割り当てます。これにより、事前にプロビジョニングされたストレージ アカウント (名前は **`sc500lab1d`** で始まります) と **sc500-lab1d-vnet** 仮想ネットワークに非準拠のフラグが設定されます。どちらのリソースにも `Environment` タグがないためです。

1. **User1** の資格情報を使用して Azure portal (`https://portal.azure.com`) にサインインします。

1. 検索バーで、**`Policy`** を検索して選択します。

1. 左側のメニューの **[作成]** で、**[割り当て]** を選択します。

1. **[ポリシーの割り当て]** を選択します。

1. **[基本]** タブで、次の設定を構成します。

    | 設定 | 値 |
    |---------|-------|
    | **Scope** | 省略記号 (**[...]**) を選択し、お使いのサブスクリプションと、リソース グループとして **[sc500-lab1d-rg]** を選択します。 **[選択]** を選択します。 |
    | **除外** | 空白のままにします |
    | **ポリシー定義** | 省略記号 (**[...]**) を選択し、**`Require a tag on resources`** を検索して結果を選択し、**[追加]** を選択します。 |
    | **割り当て名** | SC500-require-env-tag |
    | **ポリシーの適用** | Enabled |

1. **パラメーター** タブを選択します。

1. **[タグ名]** フィールドに「`Environment`」と入力します。

1. **[確認と作成](確認と作成)** を選択し、次に **[作成]** を選択します。

    > **注**: ポリシー割り当てが完全に伝達されるまで最大 30 分かかる場合があります。その後、新しい割り当てがコンプライアンス評価に反映されます。 待機せずに、次の手順でオンデマンドのスキャンをトリガーします。

1. Azure portal の上部バーで、**Cloud Shell** アイコン (>_) を選択します。 シェルの種類を選択するように求められたら、**[Bash]** を選択します。 「はじめに」ページで **[ストレージアカウントは不要です]** を選択し、ラボのサブスクリプションを選択し、**[適用]** を選択します。

1. 次のコマンドを実行して、リソース グループのオンデマンドのコンプライアンス評価をトリガーします。

    ```bash
    az policy state trigger-scan --resource-group sc500-lab1d-rg
    ```

    このコマンドは、スキャンの実行中に **IN-PROGRESS** インジケーターを表示し、スキャンが完了したときにのみ Bash プロンプトを返します。 通常、これには **5 分以上**かかりますが、サブスクリプションの負荷によってはさらに時間がかかる場合があります。  スキャンが完了するまで待つ必要はありません。 1 から 2 分待ってから、次の注と手順に進んでください。

    > **注**: コマンドの完了後もコンプライアンスの状態が**未開始**または **0 個の非準拠リソース**と表示される場合は、さらに 2 から 3 分待ってから、ポータルで **[更新]** を選択してください。 コンプライアンスの状態の更新は、スキャンの完了後に非同期に書き込まれます。

1. Azure portal で **[ポリシー]** に戻り、左側のメニューから **[コンプライアンス]** を選択します。

1. ページ上部にあるスコープ フィルターで、`sc500-lab1d-rg` を選択して、このリソース グループに絞り込みます。

1. コンプライアンスの一覧で、**[sc500-require-env-tag]** 割り当てを見つけます。

1. ストレージ アカウント (名前は **`sc500lab1d`** で始まります) と **sc500-lab1d-vnet** 仮想ネットワークが非準拠リソースの一覧に表示されていることを確認します。

    > **注**: 両方のリソースは `Environment` タグなしでデプロイされたため、ポリシーに違反しています。 このポリシー割り当てにより、今後 `sc500-lab1d-rg` 内にデプロイされるリソースで `Environment` タグが省略されるのを防ぐこともできます。 コンプライアンス評価は非破壊的であるため、既存のリソースは稼働し続けます。

---

## コードとしてのインフラストラクチャを使用してカスタム ポリシーをデプロイする

割り当てた組み込みポリシーにより、個々のリソースに対してタグ要件が強制されます。 リソース グループ レベルでの補完的なポリシーにより、サブスクリプション内で作成される新しいリソース グループにも、最初からタグが確実に付与されるようになります。 ポータルでこのポリシーを構成するのではなく、サブスクリプションのスコープでカスタム ポリシーを定義して割り当てる事前作成済みの Bicep テンプレートをデプロイします。 コードとしてのインフラストラクチャを通じてガバナンス ポリシーを導入すると、バージョン管理、再現性、監査可能性が確保されます。

**sc500-lab1d-policy.bicep** ファイルは、**F:\AllFiles\Lab-1D** フォルダーに提供されています。 このファイルは、すべてのリソース グループに対して **Environment** タグを必須とするカスタムの**拒否**ポリシーを定義し、サブスクリプション スコープの割り当てを作成します。

1. Cloud Shell (Bash) をまだ開いていない場合は開きます。

1. Bash ウィンドウの上部にあるメニューで、**[ファイルの管理]** ボタンを選択します。

1. **[アップロード]** を選択します。

1. ラボ VM 上の **F:\AllFiles\Lab-1D** フォルダーを参照し、**sc500-lab1d-policy.bicep** ファイルを選択します。

1. **[開く]** を選択して、ファイルをアップロードします。 "**ファイルが正常にアップロードされました**" メッセージを待ちます。

1. Cloud Shell (Bash) セッションで次のコマンドを実行して、カスタム ポリシーをサブスクリプション スコープにデプロイします。

    ```bash
    az deployment sub create \
      --name sc500-tag-policy \
      --location eastus \
      --template-file ~/sc500-lab1d-policy.bicep
    ```

    デプロイは通常 1 分以内に完了します。 成功すると、ターミナルに JSON 出力ブロックが表示されます。

    > **注**: `--location eastus` フラグは、リソースが作成される場所ではなく、デプロイのメタデータ レコードのリージョンを指定します。 サブスクリプション スコープの Bicep デプロイでは、作成されるリソース (ポリシー定義など) のスコープがグローバルに設定されていても、ARM メタデータの場所を指定する必要があります。

1. Azure portal で、**[ポリシー]** に移動し、左側のメニューから **[定義]** を選択します。

1. **[種類]** フィルターで、**[カスタム]** を選択します。

    **リソース グループでタグを必須とする**ポリシーを要求するカスタム ポリシー定義が一覧に表示されていることを確認します。 これは、Bicep テンプレートによってデプロイされた定義です。

    > **注**: この手順は、ポリシー ガバナンスのためにコードとしてのインフラストラクチャを使用する方法を示しています。 同一の Bicep テンプレートをリポジトリにコミットし、pull request によるレビューを行い、複数の環境に一貫してデプロイすることが可できます。これにより、ポータルでの手動による構成に依存せずに、ガバナンス ルールが均一かつ確実に適用されます。

1. 左側のメニューから **[割り当て]** を選択します。

    カスタムのタグ ポリシーの割り当てが一覧に表示され、スコープがサブスクリプションに設定されていることを確認します。 Bicep テンプレートにより、ポリシー定義と割り当ての両方が 1 回の操作だけでデプロイされました。

1. Cloud Shellを閉じます。

---

## カスタムのセキュリティ レビュー担当者ロールを作成する

**閲覧者**などの組み込みの Azure ロールは、スコープ内のすべてのリソースの種類に対する広範な読み取りアクセスを許可します。 Defender for Cloud のセキュリティ態勢データやロールの割り当ての確認など、特定のガバナンス機能のためにロールが必要な場合は、必要最小限のアクセス許可を持つカスタム役割の方が適しています。 Microsoft Defender for Cloud のデータと Azure 認証オブジェクトのみに対する読み取りアクセスを許可する **sc500-Security-Reviewer**という名前のロールを作成し、それを **User2** に割り当てます。

1. Azure portal の検索バーで、「**リソース グループ**」を検索して選択します。

1. **[sc500-lab1d-rg]** を選択します。

1. 左側のメニューで **[アクセス制御 (IAM)]** を選択します。

1. **[+ 追加]** を選択し、**[カスタム役割の追加]** を選択します。

1. **[基本]** タブで、次の設定を構成します。

    | 設定 | Value |
    |---------|-------|
    | **カスタム ロール名** | `sc500-Security-Reviewer` |
    | **説明** | `Read-only access to Defender for Cloud security posture data and role assignments. Scoped to the sc500-lab1d-rg resource group.` |
    | **ベースラインのアクセス許可** | 最初から行う |

1. **[次へ]** を選択して、**[アクセス許可]** タブに進みます。

    > **注**: **[アクセス許可]** タブには、個々の操作を追加するための検索可能なカードベースのインターフェイスが用意されています。 ワイルドカードのアクセス許可 (たとえば `Microsoft.Security/*/read`) を追加するには、ロールの JSON を直接編集する必要があります。これは **[JSON]** タブで行います。

1. **[次へ]** を選択して **[割り当て可能なスコープ]** タブに進みます。

    **sc500-lab1d-rg** が割り当て可能なスコープとして一覧に表示されていることを確認します。 リソースグループの IAM ページからカスタム役割ウィザードを開いたため、スコープはあらかじめ設定されています。 一覧にない場合は、**[割り当て可能なスコープの追加]** を選択し、お使いのサブスクリプションを展開して **[sc500-lab1d-rg]** を選択し、**[追加]** を選択します。

1. **[次へ]** を選択して、**[JSON]** タブに進みます。

1. **[編集]** を選択して、JSON エディターを開きます。

1. `"permissions"` 配列内で行 `"actions": []` を見つけます。 空の配列を次のコードに置き換えます。

    ```json
    "actions": [
        "Microsoft.Security/*/read",
        "Microsoft.Authorization/*/read"
    ],
    ```

    完成した `"permissions"` ブロックは次のようになります。

    ```json
    "permissions": [
        {
            "actions": [
                "Microsoft.Security/*/read",
                "Microsoft.Authorization/*/read"
            ],
            "notActions": [],
            "dataActions": [],
            "notDataActions": []
        }
    ]
    ```

    > **注**: `Microsoft.Security/*/read` は、Defender for Cloud および Azure Security Center のすべてのデータ (セキュリティ評価、推奨事項、アラート、セキュア スコア) に対する読み取りアクセスを許可します。 `Microsoft.Authorization/*/read` は、すべてのロールの割り当て、ポリシー割り当て、ロール定義に対する読み取りアクセスを許可します。これにより、レビュー担当者は、書き込みまたは削除のアクセス許可を持たなくても、誰が何にアクセスできるかを監査できます。

1. **[保存]** を選択して、JSON の変更を適用します。

1. **[確認と作成](確認と作成)** を選択し、次に **[作成]** を選択します。

    カスタム役割が作成されるまで待ちます。 通常、これには数秒しかかかりません。

1. `sc500-lab1d-rg` の **[アクセスの制御 (IAM)]** ページで、**[追加]**、**[ロールの割り当ての追加]** の順に選択します。

1. **[ロール]** タブで、**[種類]** フィルター ドロップダウンから **[カスタム役割]** を選択し、**[sc500-Security-Reviewer]** を選択します。 [**次へ**] を選択します。

1. **[メンバー]** タブで、**[アクセスの割り当て先]** を **[ユーザー、グループ、またはサービス プリンシパル]** に設定します。

1. **[+ メンバーの選択]** を選択し、「**User2**」を検索して選択し、**[選択]** を選択します。

1. **[レビューと割り当て]** を選択し、もう一度 **[レビューと割り当て]** を選択して保存します。

    > **注**: `sc500-Security-Reviewer` ロール自体は定義された読み取り権限のみを付与します。 このラボ環境では、**User2** は、サブスクリプションでホストされている ** LOD 共同作成者**ロールも保持するため、User2 の実質的なアクセス許可はこのカスタム ロールよりも広範囲になります。 この演習では、カスタム ロールの定義と割り当てを紹介します。運用環境では、このロールを、より広範な継承されたロールなしでプリンシパルに割り当てます。 User2 は次のセクションで指定レビュー担当者も務めます。

---

## 上昇特権アクセスを評価して修復する

**User3** は、ラボ サブスクリプショで直接アクティブな **[共同作成者]** ロールの割り当てを保持しています。 この共同作成者ロールにより、完全な管理アクセス (リソースの作成、変更、削除を行う権限) は付与されますが、ロールの割り当てを管理する権限は付与されません。 **User3** は、このアクセスをビジネス上もう必要としていません。

Microsoft Entra アクセス レビューでは、既存のロールの割り当てが引き続き適切かどうかを判断するための、構造化された監査可能なプロセスが提供されます。 シード割り当てを確認し、Privileged Identity Management (PIM) へのラボ サブスクリプションをオンボードし、**[User2]** をレビュー担当者として指定し、却下を提出し、共同作成者割り当てが削除されたことを確認します。 テナントに必要な ID ガバナンス機能が含まれていない場合は、代わりに直接修復のフォールバックを使用します。

1. Azure portal で、**[サブスクリプション]** を検索して選択し、このラボで使用されるサブスクリプションを選択します。

1. **[アクセス制御 (IAM)]** を選択した後、**[ロールの割り当て]** タブを選択します。

1. **User3** を検索し、**User3** に、スコープが **[このリソース]** の **[共同作成者]** と **[LOD 閲覧者]** の両方の割り当てがあることを確認します。

    > **重要**: 共同作成者の割り当てが見つからない場合は、ここで停止します。 ラボ セットアップのデプロイで正しい **User3 オブジェクト ID** を受け取っておらず、レビューできる割り当てがありません。 これはセットアップの問題であり、ロール割り当ての伝達の問題ではありません。

1. Microsoft [Entra 管理センター](https://entra.microsoft.com)に移動します。

1. ポータルの検索バーで **Privileged Identity Management** を検索し、**Privileged Identity Management Browse** を選択します。

1. **[参照]** ページの **[サブスクリプション]** の下の、ラボ サブスクリプションを選択します。

1. **[現在の選択]** に、ラボ サブスクリプションが表示されていることを確認してから、**[リソースの管理]** を選択します。

    > **注**: オンボードすると、サブスクリプションの PIM の概要が開き、特権割り当ての管理に必要なアクセス権が PIM サービス プリンシパルに付与されます。

1. **[管理]** の下で **[アクセス レビュー]** を選択します。

    > **重要**: Azure リソース ロール アクセス レビューには Microsoft Entra ID P2 または Microsoft Entra ID ガバナンスが必要です。 レビューを作成するユーザーには、所有者またはユーザー アクセス管理者のアクセス許可も必要です。 このホストされているラボでは、**User1** が **LOD 所有者**ロールを保持しています。 ページに **[アクセス許可がありません]** と表示される場合、ロールの伝達を待機してもメッセージは解決されません。 「**フォールバック: 割り当てを直接削除する**」までスキップしてください。

### アクセス レビューを作成して完了する

この項は **[アクセス レビュー]** ページが正常に開いた場合にのみ完了してください。

1. **新規**を選択します。

1. **[アクセス レビューの作成]** ページで、レビューの [詳細] を次のように構成します。

    | 設定 | 値 |
    |---------|-------|
    | **レビュー名** | `sc500-contributor-review` |
    | **説明** | `Review of Contributor access on the lab subscription` |
    | **開始日** | 今日の日付 |
    | **頻度** | 指定日時 |
    | **期間 (日数)** | 3 |

1. **[ユーザーのスコープ]** の下で **[ユーザー]** を選択します。

1. **[ロールメンバーシップの確認]** で、**[共同作成者]** を選択します。

1. **[割り当てタイプ]** で、**[アクティブな割り当てのみ]** を選択します。

1. **[レビュー担当者]** で **[選択されているユーザーまたはグループ]** を選択し、**[レビュー担当者の選択]** を選択します。

1. **[User2]** を選択し、選択を確認します。

    > **重要**: **[レビュー担当者の選択]** ペインが 10 秒後も空白のままの場合は、ペインを閉じて **[フォールバック: 割り当てを直接削除する]** にスキップします。 これは、ホステッドテナント ピッカーの問題です。ロールの伝達を待機しても、ペインにデータは入力されません。

1. **[設定完了時]** を展開して次を設定します。

    | 設定 | Value |
    |---------|-------|
    | **リソースに結果を自動適用する** | Enable |
    | **レビュー担当者が応答しない場合** | 変更なし |

1. **[開始]** を選択して、アクセス レビューを作成してアクティブにします。

1. 新しい **InPrivate** または **Private** ブラウザー ウィンドウを開き、**User2** に指定された認証情報で `https://entra.microsoft.com` にサインインします。

1. **[ID ガバナンス] > [Privileged Identity Management] > [アクセスのレビュー]** の順に移動します。

1. **sc500-contributor-review** を選択します。 **User3** のエントリを選択し、**[拒否]** を選択し、理由を「`No current business need for Contributor access`」と入力して、決定を送信します。

1. InPrivate ウィンドウを閉じて **User1** セッションに戻ります。

1. **[Privileged Identity Management Browse]** に戻り、ラボ サブスクリプションを選択して、**[リソースの管理]** を選択し、**[アクセス レビュー]** を選択します。

1. **[sc500-contributor-review]** を選択し、**[停止]** を選択して操作を確認します。

1. レビューの状態が **[完了]** または **[適用中]** から **[適用済み]** に変わるまで更新してください。 通常、これには数分かかります。

1. **[修復の確認]** までスキップしてください。

### フォールバック: 割り当てを直接削除する

**[アクセス レビュー]** ページに **[アクセス権がありません]** と表示されている場合、または **[レビュー担当者の選択]** ペインが空白の場合は、このサブセクションを完了してください。

1. Azure portal のラボ サブスクリプションに戻ります。

1. **[アクセス制御 (IAM)] > [ロールの割り当て]** の順に選択します。

1. **User3** を検索し、範囲が **[このリソース]** の **[共同作成者]** 割り当ての選択ボックスを選択し、コマンド バーの **[削除]** を選択します。 削除を確定します。

    > **注**: 直接削除によって上昇特権アクセスは修復されますが、アクセス レビュー監査レコードは作成されません。 本番環境では、必要な Microsoft Entra ID ガバナンス ライセンスを有効にし、正式なアクセス認定のために定期的なアクセス レビューを使用してください。

### 修復を確認する

1. ラボ サブスクリプションで、**[アクセス制御 (IAM)] > [ロールの割り当て]** に戻ります。 ページを更新して **User3** を検索します。

1. **User3** に **[共同作成者]** ロールがなくなったことを確認します。 ベースライン **[LOD 閲覧者]** 割り当ては維持されており、これは想定されているものです。

    > **注**: アクセス レビューのパスを完了すると、完了済みのレビューにレビュー担当者、判断、正当な理由、アクセス変更の結果の監査可能なタイムスタンプ付きレコードが保持されます。

---

## リソース ロックを適用する

リソース ロックは、重要なリソースの偶発的または不正な削除を防ぎます。 **CanNotDelete** ロックでは、リソースに対するすべての読み書き操作が許可されますが、所有者ロールを持つユーザーであっても削除操作がブロックされます。 `sc500-lab1d-rg` 内の事前にプロビジョニングされたストレージ アカウントにロックを適用し、そのロックによって削除が防止されることを確認します。

> **注**: `sc500-lab1d-rg` リソース グループにはストレージ アカウントが 1 つだけ含まれており、その名前は **`sc500lab1d`** で始まり、その後にラボ サブスクリプションに固有の 8 文字のサフィックスが続きます。 ラボで参照する場合はいつでもそれを選択します。

1. Azure portal の検索バーで、「**ストレージ アカウント**」を検索して選択します。

1. 名前が **`sc500lab1d`** で始まるストレージ アカウントを選択します (`sc500-lab1d-rg` 内には 1 つしかありません)。

1. 左側のメニューの **[設定]** で、**[ロック]** を選択します。

1. **[+ 追加]** を選択します。

1. 次を構成します。

    | 設定 | 値 |
    |---------|-------|
    | **ロック名** | `sc500-storage-lock` |
    | **ロックの種類** | 削除 |
    | **ノート** | `Prevents accidental deletion of the AI platform storage account.` |

1. **[OK]** を選択します。

1. 左側のメニューで、 **[概要]** を選択します。

1. **[削除]** を選択します。

1. 確認ダイアログで、ストレージ アカウント名を入力して確認し、**[削除]** を選択します。

    リソースがロックされていて削除できないことを示すエラーメッセージが表示されることを確認します。

    > **注**: `CanNotDelete` ロックは、要求元のロールに関係なく、Azure Resource Manager によって適用されます。 所有者またはサブスクリプション管理者は、ロックが設定されている間はこのリソースを削除できません。最初にロックを明示的に解除する必要があります。 これにより、意図的な摩擦が生じ、自動化されたスクリプトや正しく設定されていないパイプラインによって重要なリソースが破壊されるのを防ぐことができます。 `ReadOnly` ロックはより厳格であり、すべての書き込みおよび削除の操作がブロックされます。ただし、リソース メタデータの更新を正当に必要とするプラットフォーム操作を妨げる可能性があります。 `CanNotDelete` は、ほとんどの運用リソース保護シナリオで推奨される選択肢です。

1. [削除の確認] ダイアログを閉じます。

---

## まとめ

このラボでは、ポリシー コンプライアンス、コードとしてのインフラストラクチャによるポリシーのデプロイ、カスタム役割の作成、アクセス認定という 4 つの側面でガバナンス コントロールを適用しました。

**リソースでタグを必須とする**組み込みポリシーを割り当てて、`Environment` タグがない既存の非準拠リソースを明らかにし、標準の評価サイクルを待つ代わりにオンデマンドのコンプライアンス スキャンをトリガーして結果を即座に観察しました。 その後、あらかじめ作成された Bicep テンプレートを使用して、サブスクリプション スコープで補完的なカスタム ポリシーをデプロイしました。これにより、ガバナンス ルールもアプリケーションコードと同様に、コードとしてのインフラストラクチャを通じてバージョン管理でき、繰り返しデプロイできることを示しました。

カスタム Azure ロール `sc500-Security-Reviewer` を作成しました。この定義には、セキュリティ監査機能に必要な読み取りアクセス許可のみが定義に含まれています。 これを **User2** に割り当て、カスタム ロールによって付与されたアクセス許可と、ラボサブスクリプションの User2 のより広範な LOD 共同作成者アクセス権を区別しました。

テナントで Entra ID ガバナンスがサポートされている場合は、Entra ID のアクセス レビューを使用して、**User3** の有効な共同作成者割り当てを正式に評価しました。 そのレビュー パスにより、レビュー担当者、正当な理由、判断、その結果としての行動の監査可能なレコードが作成されました。 アクセス レビューが利用できなかった場合は、Azure RBAC を使用して割り当てを直接削除し、正式なアクセス認証でなぜ追加のガバナンス機能が必要なのかを文書化しました。 最後に、プラットフォーム ストレージ アカウントにリソース ロック CanNotDelete を適用しました。これにより、ID ベースのアクセス制御とリソース ロックが相互に補完的な役割を果たすことを示しました。アクセス制御は、誰が操作できるかを管理し、リソース ロックは、意図的な解除手順なしで、高度な特権を持つ ID であってもバイパスできない明示的なバリアを作成します。

この演習は無事に完了しました。

---

## クリーンアップ

ラボ環境は、セッション終了時に自動的にリセットされます。 手動でリソースを削除する必要はありません。
